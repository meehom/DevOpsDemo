---
id: REQ-XXX
title: 一句话说明这个需求要做什么
status: draft
owner: 你的名字
created: YYYY-MM-DD
---

## 背景

为什么要做这个需求，当前有什么问题，谁受影响。

## 目标

做完成什么样。用结果描述，不要写实现方案。

## 验收标准

每条都必须是可测的、能翻译成断言的行为。编号后不要删，测试里对应引用。

- [ ] AC-1: 调用 `GET /add?a=2&b=3` 返回 `{"result": 5.0}`
- [ ] AC-2: 缺少参数时返回 422
- [ ] AC-3: 页面输入 2 和 3 点击「计算」后显示 `5`

## 非目标

明确不做什么，防止范围蔓延。

## 影响范围

新增/修改的接口、页面、数据模型。

## 测试映射

- 单元测试: `tests/test_req_xxx_*.py`
- 端到端测试: `tests/e2e/test_req_xxx_*.py`